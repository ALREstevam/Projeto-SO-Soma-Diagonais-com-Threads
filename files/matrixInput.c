#ifndef matrixInput_C
#define matrixInput_C

#include <stdio.h>
#include "../dataStructures/matrixDymnAlloc.h"
#include "../global.h"


/*
void fileToMatrix(char filename[30], MatrixDescriber *mxDesc){
	float num;
	FILE *arq;
	arq = fopen(filename, 'r');
	
	
	while(!feof(arq)){
		fread(&num, sizeof(num), 1, arq);
		printf("LENDO: %f\n", num);
	}	
}*/


#endif